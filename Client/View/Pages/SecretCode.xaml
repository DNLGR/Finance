<Page x:Class="Client.View.Pages.SecretCode"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Client.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SecretCode">

    <Page.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedDarkButton}">
            <Setter Property="Margin" Value="5" />
        </Style>

        <Style TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
            <Setter Property="Margin" Value="5" />
        </Style>
    </Page.Resources>
    
    <Grid>
        <Border BorderBrush="{StaticResource PrimaryHueMidBrush}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Запишите секретный код, для восстановления ваших учетных данных" Margin="0 0 0 10" />
                <TextBlock Text="{Binding User_secret_code}" />
                <Button Content="OK" Width="140" Name="BtnAccept" Click="BtnAccept_Click" />
            </StackPanel>
        </Border>

    </Grid>
</Page>
