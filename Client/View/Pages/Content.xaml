<Page x:Class="Client.View.Pages.Content"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Client.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="398" 
      d:DesignWidth="800"
      Title="Content">

    <Page.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignIconButton}">
            <Setter Property="Margin" Value="0 5 0 5" />
        </Style>
    </Page.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Vertical"
                    VerticalAlignment="Center">
            
            <Button x:Name="ButtonAccountChange" Content="{materialDesign:PackIcon Kind=AccountConvert}" Click="AccountChange_Click" />

            <Button x:Name="ButtonMainTable" Content="{materialDesign:PackIcon Kind=Table}" Click="ButtonTable_Click" />

        </StackPanel>
    </Grid>
</Page>
